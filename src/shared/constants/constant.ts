// Throttling configuration constants
export const THROTTLER_TTL = 0;
export const THROTTLER_LIMIT = 0;

// Authentication rate limits
export const AUTH_RATE_LIMIT_TTL = 900000;
export const AUTH_RATE_LIMIT = 15;

// File configuration constants
export const MAX_FILE_SIZE = 10 * 1000 * 1000; // size in bytes
export const ACCEPTED_FILE_TYPES: string[] = ['image/jpeg', 'image/jpg', 'image/png'];

export const LIMIT = '10';
export const OFFSET = '0';
